package com.example.black_box;// Generated by Selenium IDE
import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Alert;
import org.openqa.selenium.Keys;
import java.util.*;
import java.net.MalformedURLException;
import java.net.URL;
public class SuggestedrecipematcheswithlinkedRecipeTest {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
    driver = new ChromeDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  @Test
  public void suggestedrecipematcheswithlinkedRecipe() {
    driver.get("http://localhost:8080/homepage");
    driver.manage().window().setSize(new Dimension(1792, 1120));
    driver.findElement(By.id("height")).click();
    driver.findElement(By.id("height")).sendKeys("177");
    driver.findElement(By.id("weight")).click();
    driver.findElement(By.id("weight")).sendKeys("90");
    driver.findElement(By.id("age")).click();
    driver.findElement(By.id("age")).sendKeys("20");
    driver.findElement(By.id("1")).click();
    driver.findElement(By.id("4")).click();
    driver.findElement(By.id("7")).click();
    driver.findElement(By.id("btn")).click();
    driver.findElement(By.cssSelector(".column:nth-child(3) h2")).click();
    vars.put("preRecipe", driver.findElement(By.cssSelector(".column:nth-child(3) h2")).getText());
    driver.findElement(By.linkText("Click To View")).click();
    vars.put("postRecipe", driver.findElement(By.cssSelector(".heading-1")).getText());
    if ((Boolean) js.executeScript("return (arguments[0] === arguments[1])", vars.get("preRecipe"),vars.get("postRecipe"))) {
    }
    driver.close();
  }
}
